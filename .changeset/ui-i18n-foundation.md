---
"@jmfederico/pi-web": patch
---

Add a lightweight English / Simplified Chinese interface language setting under Settings → General → Interface, following the browser language by default. The change applies immediately and is stored per browser.
