---
"@jmfederico/pi-web": patch
---

Add a tokens-per-second rate to the session status bar (live while streaming, last message average when idle) and a plan-mode toggle button next to the model/thinking pickers when a plan extension command is installed.
