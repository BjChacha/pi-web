---
"@jmfederico/pi-web": patch
---

Redesign the chat composer area: the session context (project, workspace, worktree badge) now shows in a header strip above the composer with workspace actions (copy path/branch, delete worktree), the prompt editor, model/thinking controls, and actions are merged into one elevated card, and the status bar strings follow the interface language setting.
